# Project Status

Last updated: 2026-04-22

## Honest Current State

**The product claim is "first-class orchestration: follow, review, intervene."
That claim is now broadly true for the follow/review surfaces.** Follow is real
through SSE plus replay folding; review has deep artifact and usage provenance;
intervene is durable and re-enters the packet flow. The April 22 audit wave
closed the main cohesion and concurrency findings, and M07 (Artifact Preview
Depth) is fully landed as of this session — both the demo-ceremony half (A-10
slices 1–3) and the artifact-preview half (M07 slices 1–4). Every canonical
artifact write (`sprint-plan.md`, `TEST_REPORT.md`, `REVIEW.md`, `DEPLOY.md`,
narrative-digest `.md`) now emits an `artifact_preview_created` event with
sha256, bounded excerpts, secret scrubbing, A-9 provenance, `producing_atom_id`
anchor, and `PHASE_SOURCE_REFS` lineage; the PWA folds those into
`snapshot.previews` and renders `artifactPreviewCard` in Story + Object Lens
rails anchored by atom. That means every atom in the replay view now shows the
bytes it actually produced — not a summary of them. **The load-bearing remaining
work is M08 Memory Spine**, which has a solid backend foundation but is missing
the PWA surface, cross-phase memory diff signalling, and the memory-refs thread
into narrative/DWA packets. M08 is the next milestone.

### What actually works

- **Follow.** The backend emits durable events and SSE replay buffers. The PWA
  folds those events through `EventEngine` into live and replay snapshots.
- **Review.** Opencode session exports are translated into `raw-io.jsonl` and
  `usage.jsonl` with shared join keys. Narrative digests and discussion logs
  cite source refs instead of inventing summaries without provenance.
  **Artifact previews** now round this out: every canonical role artifact is
  replay-visible as a card with hash, excerpt, and source lineage.
- **Intervene.** `operator_intent` events persist through
  `.orchestrator/control/intents.jsonl`, appear on next-step packets, and
  become `operator_intent_applied` or `operator_intent_ignored`.
- **PWA map.** The Three.js game surface now has Next-Step, Object, Story, and
  Goal lenses plus narrator dialogue and compose controls on top of the same
  replay snapshot. Both lenses gained demo-ceremony cards (A-10) and
  artifact-preview cards (M07) in this April 22 wave.
- **Dry-run pipeline.** `dry_run=True` auto-wires deterministic role runners.
  This is the default local proof path when no live model provider is present.
- **Live runtime.** Production role work is routed through opencode
  `SessionRunner`s built from `endpoints:` and `routing:` config.
- **Memory plumbing (backend).** MemPalace init/mine/wake-up/search/status all
  land; `wake_up_hash` + `memory_refs` are stamped onto `memory_refreshed`,
  `memory_error`, `next_step_packet_created`, every demo event, and every
  `artifact_preview_created` event; every opencode-backed role injects the
  wake-up packet into its prompt.

### Load-bearing gaps

1. **Full live attended run still needs proof.** The opencode production path
   has a fake-binary guardrail, but the operator still needs a real provider
   walkthrough with gates enabled and PWA decisions in the loop.
2. **Live provider confidence.** Usage rows now share one payload path across
   advisor and opencode calls, but a real attended provider walkthrough still
   needs to prove cost/runtime telemetry under live failure and retry behavior.
3. **Memory surface is invisible in the PWA.** M08's backend provenance is
   solid, but the operator cannot see it — there is no memory body on the
   goal core orbit, no memory panel (status / last search / sync control /
   wake-up preview), no cross-phase diff signal, and no memory-refs thread
   into narrative digests or discussion. This is the next shippable wave.
4. **Founding-team cadence and intervention depth.** Intents are durable and
   replayable. A-8 now has a bounded consultation design, including temporary
   Hero reviewers, but the recurring consultation implementation still needs
   to land.
5. **Demo-ceremony live-browser proof.** A-10 infrastructure is complete, but
   every test still uses a fake Playwright runner; wiring a real spec into a
   shipped mini-sprint is the remaining proof. Optional ffmpeg GIF derivation
   from `interaction.webm` is also deferred. M11 owns the replay/export
   bundling of those artifacts.

## Phase Board

The old H-series board is archival context. The live execution board is the
M-series plan in `project-management/srs-mini-phases/`; those files are the
running checklist for product-grade follow/review/intervene work.

| Phase | Status | Owner | Notes |
| --- | --- | --- | --- |
| **M01 Event Ledger** | Mostly done | Codex/Claude | Durable events, SSE replay, EventEngine folding, raw audit rows, central event registry, replay-buffer pruning, and atomic run registration exist. |
| **M02 Intent Lifecycle** | Done first pass | Codex | Durable queued/applied/ignored intents, client intent ids, next-step attachment, prompt application, and PWA compose affordances landed. Needs deeper live-run UX hardening. |
| **M03 Mythos/Founding Team** | Partial | Codex | Persona/forum material feeds discussion and Goal Core. A-8 now defines bounded recurring consultations plus temporary Hero reviewers; implementation still pending. |
| **M04 Narrative Digest/DWA** | Done first pass | Codex | Deterministic narrative digests, discussion projection, source refs, and replay fold exist. |
| **M05 Narrator Agent** | Done first pass | Codex | Read-only narrator role, sidecar triggers, fallback diagnostics, sidecar state locking, outcome normalization, and PWA dialogue shell exist. |
| **M06 PWA Lenses/Map Surface** | Done first pass | Codex | Next-Step, Object, Story, and Goal lenses are in the map surface with WebGL fallback coverage. |
| **M07 Artifact Preview Depth** | Complete | Claude | Demo-ceremony half via A-10 slices 1/2/2b/3 (demo events + storage + `DemoRunner` + bounded `/api/projects/{id}/demos/...` route + Story/Object Lens demo cards). Artifact-preview half landed via M07 slices 1–4: `artifact_preview_created` emitter with sha256, bounded excerpts (8 KiB text head / 2 KiB code tail / binary placeholder), secret scrubbing, A-9 provenance; engine + narrator hooks emit previews for every canonical artifact (sprint-plan, TEST_REPORT, REVIEW, DEPLOY, narrative digest `.md`); PWA folds previews into `snapshot.previews`, renders `artifactPreviewCard` in Story + Object Lens rails anchored by `producingAtomId`; `PHASE_SOURCE_REFS` threads upstream lineage through every event. |
| **M08 Memory Spine** | Partial | Codex | MemPalace sync/search/provenance packets exist and are attached to next-step context. Needs broader UI treatment and stronger contract checks. |
| **M09 Cost/Budget Telemetry** | Partial | Claude | Usage rows and rollups exist. Needs PWA budget controls and provider-aware display. |
| **M10 Hookability** | Not started | Unassigned | External trigger/plugin boundary still needs design and tests. |
| **M11 Replay Resilience** | Partial | Codex | Event replay works; needs corruption/rebuild/fallback hardening and replay/export handling for recorded demo artifacts. |
| **M12 Release Hardening** | Not started | Unassigned | Requires live attended run, mobile polish, accessibility pass, and packaging checks. |

## Next Recommended Task

**Next milestone: M08 Memory Spine.** With M07 artifact previews fully landed,
the largest remaining visible gap is that MemPalace state is invisible in the
PWA. Backend provenance (`wake_up_hash`, `memory_refs`, memory events) is
already stamped everywhere that matters; what is missing is the operator-facing
surface and the cross-phase diff signal. See
`project-management/srs-mini-phases/M08-memory-spine-and-mempalace.md` for the
full checklist.

**Recommended slice order (each shippable independently):**

1. **M08 slice A — PWA memory surface** (highest-impact, pure frontend + a
   bounded backend route). Add a low-frequency `memory-core:body` on the goal
   orbit (pattern established by M06C Goal Core). Tap opens a memory sheet
   showing: MemPalace status (initialized, wing, command availability), latest
   `wake_up_hash` with a truncated wake-up preview, last search (scrubbed),
   memory sync button that POSTs to the existing memory route, and a recent
   list of `memory_refreshed` / `memory_search_completed` / `memory_error`
   events folded from the snapshot. Fold already exists in `EventEngine`
   (`memory_status`, `memory_refreshed`, `memory_search_completed`); render
   them. Test with a new `test_game_map.py` jsdom fixture + bundle assertions.

2. **M08 slice B — cross-phase memory diff event.** After each phase ends,
   compare the current wake-up hash to the last emitted one and emit a
   `memory_refreshed` (or a new `memory_diff` event if it's cleaner) with a
   `hash_changed` flag. This lets the PWA show "memory shifted between
   architect and testing" without any semantic diff. The orchestrator has the
   previous hash in the last `memory_refreshed` event on the bus — fold it or
   re-read `.orchestrator/memory/wake-up.md` and compare sha256.

3. **M08 slice C — memory refs on narrative digests and discussion entries.**
   `narrative_digest_created` already carries `wake_up_hash` + `memory_refs`
   through `memory_event_fields`; verify the audit + extend to
   `discussion_entry_created` and `discussion_highlight_created` so Story
   Lens rows cite the memory packet that shaped them. Checklist item
   currently blocked on "DWA/narrative digest refs remain pending until
   M04/M05."

4. **M08 slice D — quality gates G2/G4/G5/G7 for memory.** Replay
   determinism (G2) requires fold to use the `wake_up_hash` present at the
   event index, not current palace state — audit the fold path. G4 requires
   the memory surface be a body + panel, not a tab grid (will be satisfied
   by slice A). G5 requires every memory-backed claim cite a ref (extends
   slice C). G7 requires memory operations that invoke embedding models to
   emit `usage_recorded` — may be empty today; audit.

**Headless/scriptable path for this session:** Continue with M08 slice A
(frontend memory surface) — matches the same "land the operator-visible
surface first" shape as M07 slice 3 and M06C.

**When a human is at the device with providers ready:** run the attended PWA
walkthrough with `--gate`, real opencode providers, visible gate decisions,
intent submission before/after a phase boundary, audit-log inspection, and —
once M08 slice A lands — a live memory sync via the new memory panel to prove
the `memory_refreshed` event flows end-to-end with a provider in the loop.

## Current Verification Commands

Run from `build/workspace/` (repo root). These are the narrowest commands that
cover each surface; use them for UI/asset edits instead of the full suite.

| Surface | Command |
| --- | --- |
| Backend event + static asset serving | `uv run pytest -q web/backend/tests/test_pwa_serving.py web/backend/tests/test_events.py` |
| Frontend game asset manager | `uv run pytest -q web/frontend/tests/test_asset_manager.py` |
| Frontend map route | `uv run pytest -q web/frontend/tests/test_game_map.py` |
| Full frontend bundle | `./web/frontend/build.sh` |
| Full backend + frontend test suite | `uv run pytest -q` |

All four narrow commands verified green on 2026-04-21 (`git diff --check` clean).

## Latest Asset Facts

Ultimate Space Kit:

- Source: https://poly.pizza/bundle/Ultimate-Space-Kit-YWh743lqGX
- Creator: Quaternius
- License: CC0 1.0 Public Domain
- 87 GLB models
- 11,361,152 bytes extracted
- Animated: 3 astronauts, 4 mechs, 3 enemies
- Manifest: `assets/poly-pizza/ultimate-space-kit/manifest.json`

## Known Risks

### Runtime

- Live provider operation still needs an attended PWA walkthrough with gates on.
- Opencode role sessions are audited, but production failure handling needs more
  stress coverage around partial exports, provider stalls, and operator aborts.
- Narrator sidecar triggering is now locked and outcome-normalized, but live
  provider sidecar runs still need stress coverage around malformed output,
  retry timing, and partial audit exports.

### Product

- The map lenses are first-pass surfaces; artifact previews and source drill-in
  need more depth before the PWA feels complete.
- Full model-kit loading must stay bounded on mobile.
- Operator intents are durable, but the UI still needs stronger affordances for
  what can be changed before a phase starts, during a phase, and after a phase
  is already past.
- DOM overlays for readable previews must be pooled and collision-managed.

## Evidence Log

Append new evidence here instead of burying it in chat.

| Date | Phase/task | Evidence |
| --- | --- | --- |
| 2026-04-22 | M07 slice 4 — source_refs lineage on artifact previews | Commit `8da1ad5`. `orchestrator/lib/artifact_preview.py::PHASE_SOURCE_REFS` (static map: architect_plan/architect_refine→(qwendea.md, PERSONA_FORUM.md); testing→(sprint-plan.md,); review→(sprint-plan.md, TEST_REPORT.md); deployment→(REVIEW.md,)) is threaded through `emit_phase_artifact_previews` onto every `artifact_preview_created` event. Declarative map chosen over per-role return-value threading because the consumption pattern is a workflow contract, not a runtime property; roles wanting non-canonical source_refs can still emit their own preview events. New `test_emit_phase_artifact_previews_threads_source_refs`, `test_phase_source_refs_mapping_covers_expected_roles`; integration test extended to assert every per-phase preview carries the expected source_refs in the dry-run pipeline. Evidence: `uv run pytest -q` → 592 passed, 2 warnings |
| 2026-04-22 | M07 slice 3 — artifact preview cards folded into PWA | Commit `9cee3d8`. `web/frontend/src/game/types.ts` adds `ArtifactPreview` type + `previews` / `latestPreview` on `EngineSnapshot`. `EventEngine.applyArtifactPreviewEvent` folds `artifact_preview_created` into `snapshot.previews`, deduping by `(artifactRef, producingAtomId)` so re-writes overwrite rather than stack; previews reset across `loadHistory` / `scrubTo` / `rebuildFromHistory`. `GameShell.artifactPreviewCard(preview)` renders a card with escaped excerpt in `<pre>` for text (head/tail), italic placeholder for binary, truncation chip, and atom/mime/hash/source chips. Wired as `data-role="story-preview-rail"` in Story Lens (`snapshot.previews.slice(-3).reverse()`) and `data-role="object-preview-rail"` in Object Lens (`snapshot.previews.filter(p => p.producingAtomId === atomId).slice(-2).reverse()`). Evidence: `uv run pytest -q web/frontend/tests/test_game_map.py` → 9 passed (new `test_artifact_preview_card_helper` + 5 bundle assertions for `artifactPreviewCard` / `story-preview-rail` / `object-preview-rail` / `preview-card` / `preview-excerpt`). `./web/frontend/build.sh` → `dist/app.js` 1,331,852 bytes (+5,939 from slice 2 baseline) |
| 2026-04-22 | M07 slices 1+2 — artifact_preview_created emitter + engine/narrator hooks | Commit `157a788`. `orchestrator/lib/artifact_preview.py` adds `emit_artifact_preview` (sha256, 8 KiB text head / 2 KiB code tail / binary placeholder excerpts, secret scrubbing via `scrub_text`, path-traversal rejection, A-9 provenance from `memory_event_fields`) and `emit_phase_artifact_previews` (static `PHASE_CANONICAL_ARTIFACTS` map: architect*→sprint-plan.md, testing→TEST_REPORT.md, review→REVIEW.md, deployment→DEPLOY.md). `ARTIFACT_PREVIEW_CREATED` registered in `event_types.EVENT_TYPES` and mirrored in `web/frontend/src/game/eventTypes.ts`. Engine calls `emit_phase_artifact_previews` after each `phase_done` (wrapped in try/except — preview failure never breaks a phase). `orchestrator/lib/narrative.py::emit_narrative_digest` and `orchestrator/roles/narrator.py::_emit_digest` both emit previews for digest `.md` with `producing_atom_id="narrator"`. Evidence: `uv run pytest -q` → 590 passed, 2 warnings (11 new unit in `tests/test_artifact_preview.py` covering hashing/head-tail/binary/missing/traversal/empty/scrub/phase-mapping/unknown-phase + 1 integration `tests/test_integration_artifact_previews.py` driving dry-run and asserting sha256 + producing_atom_id per artifact + narrator preview). `./web/frontend/build.sh` → `dist/app.js` 1,325,913 bytes |
| 2026-04-22 | A-10 slice 3 — Story / Object Lens demo cards | Commit `2df8d3f`. `web/frontend/src/game/GameShell.ts` exports `demoArtifactUrl(projectId, demo, artifact)` (strips `.orchestrator/demos/<demo_id>/` prefix, URL-encodes segments → `/api/projects/{id}/demos/{demo_id}/{rest}`) and `storyDemoCard(demo, projectId)` (data-role demo-card with status chip, gif/screenshot thumbnail, artifact/source/memory/test/wake chips, failure note). Story Lens gains `data-role="story-demo-rail"` showing `snapshot.demos.slice(-3).reverse()`; Object Lens gains `data-role="object-demo-rail"` showing `snapshot.demos.filter(d => d.atomId === atomId).slice(-2).reverse()`. Evidence: `uv run pytest -q web/frontend/tests/test_game_map.py` → 8 passed (new `test_demo_artifact_url_and_card_helpers` + 5 bundle assertions in `test_game_shell_bundle_contains_replay_surface`). Full suite `uv run pytest -q` → 578 passed, 2 warnings. `./web/frontend/build.sh` → `dist/app.js` 1,325,883 bytes. `git diff --check` clean |
| 2026-04-22 | A-10 slice 2b — demo runner + bounded PWA artifact route | Commit `830e37a`. `orchestrator/lib/demo_runner.py` drives a prior `demo_planned` end-to-end: emits `demo_capture_started`, spawns `start_command`, probes `route` until ready, runs `test_command`, rglobs Playwright output for `capture_policy` matches (gif/webm/trace/screenshot defaults, overridable), hashes each via slice-2 `record_demo_artifact`, writes `.orchestrator/demos/<demo_id>/metadata.json` with plan+capture event ids, log tail, artifact refs, A-9 provenance, then emits `demo_presented` or `demo_capture_failed`. Server terminated in `finally` on every exit. `RunnerDeps` dataclass injects subprocess / readiness-probe / test-runner shims; defaults use `subprocess.Popen` + `urllib.request` + `subprocess.run`. `web/backend/routes/demo_artifacts.py` exposes `GET /api/projects/{id}/demos/{demo_id}/{name:path}` reading from `.orchestrator/demos/<demo_id>/`, rejecting traversal, enforcing `demo_<hex16>` id shape, returning `FileResponse` with MIME guessed from filename; registered in `web/backend/app.py`. Evidence: `uv run pytest -q tests/test_demo_runner.py` → 9 passed; `uv run pytest -q web/backend/tests/test_demo_artifacts.py` → 5 passed; full suite `uv run pytest -q` → 578 passed, 2 warnings. `git diff --check` clean |
| 2026-04-22 | A-10 slice 2 — demo capture + artifact lifecycle events | Commit `d5215df`. `orchestrator/lib/demo_capture.py` added ledger-side ceremony: `emit_demo_capture_started` inherits plan fields + carries viewport; `record_demo_artifact` hashes on-disk artifact (sha256 + byte count), derives project-relative `artifact_ref`, emits `demo_artifact_created` with optional `test_status`; `emit_demo_capture_failed` carries error + partial refs; `emit_demo_presented` closes with `summary_ref` + final `artifact_refs` and refuses empty lists. `write_demo_metadata(project_dir, demo_id, payload)` produces `.orchestrator/demos/<demo_id>/metadata.json` deterministically. Every event stamped with A-9 `wake_up_hash`/`memory_refs`. Event registry: `demo_capture_started`/`demo_artifact_created`/`demo_capture_failed`/`demo_presented` registered in `orchestrator/lib/event_types.py` and mirrored in `web/frontend/src/game/eventTypes.ts` (mirror assertion in `tests/test_event_schema.py` green). Frontend fold: `DemoPlan` carries `status` across planned→capturing→captured→presented (and failed), `DemoArtifact[]` list, merged `artifactRefs`, `testStatus`, `captureError`, `summaryRef`, captured `viewport`; fold and scrub/replay idempotent on `demoId`; artifacts dedupe on `artifact_ref` or `event_id`. Evidence: `uv run pytest -q tests/test_demo_capture.py tests/test_demo.py tests/test_event_schema.py` → 23 passed; `uv run pytest -q web/frontend/tests/test_event_engine.py` → 14 passed (new `test_demo_capture_lifecycle_fold`, `test_demo_capture_failed_fold`). Full suite → 564 passed, 2 warnings. `./web/frontend/build.sh` → `dist/app.js` 1,318,786 bytes |
| 2026-04-22 | A-10 slice 1 — demo event schema + playwright_web adapter plan | Commit `9e8b5bc`. `orchestrator/lib/demo.py` added: `detect_playwright_demo_plan(...)` returns an adapter plan with stable confidence (`explicit_playwright`/`operator_requested`/`inferred_interactive_web`/`static_visual`/`not_demoable`). `emit_demo_plan(...)` emits `demo_planned` or `demo_skipped` with a deterministic `demo_id` seeded from `(task_id|atom_id, adapter, spec_path, trigger_event_id)` so retries stamp the same id. A-9 provenance (`wake_up_hash`, `memory_refs`) stamped on every demo event. Event registry: `demo_planned` + `demo_skipped` registered in `orchestrator/lib/event_types.py` and mirrored in `web/frontend/src/game/eventTypes.ts`. Frontend fold: `DemoPlan` snapshot slot (`demos`, `latestDemo`) handled by `EventEngine.applyDemoEvent(...)`, preserved through scrub/rebuild. Evidence: `uv run pytest -q tests/test_demo.py tests/test_event_schema.py` → 12 passed; `uv run pytest -q web/frontend/tests/test_event_engine.py` → 12 passed (new `test_demo_events_fold`). Full suite → 553 passed, 2 warnings. `./web/frontend/build.sh` → `dist/app.js` 1,313,338 bytes |
| 2026-04-22 | Design A-10 — demo ceremonies and replay evidence | Added `docs/demo-ceremony-and-replay-evidence.md`, defining optional demo ceremonies for visually demoable mini-sprint work: adapter-based detection from acceptance/evidence text, Playwright-rooted GIF/video/trace/screenshot evidence for web/PWA tasks, durable demo events, `.orchestrator/demos/<demo_id>/` artifact layout, PWA demo cards, and replay/export rules that use recorded artifacts instead of re-running the app. Threaded the design into M07 artifact previews, M11 replay resilience, M12 release hardening, the latest brain dump handover, and the audit as A-10. Evidence: docs-only; `git diff --check` clean |
| 2026-04-22 | Feedback X-2 — SSE subscriber pruning | Replaced silent subscriber append failure handling in `SimpleEventBus.emit` with a warning and dead-subscriber pruning, and renamed the replay eviction loop variable to avoid shadowing the newly emitted `event_id`. Added `web/backend/tests/test_stream.py::TestSimpleEventBus::test_emit_prunes_failed_subscribers`. Evidence: `uv run pytest -q web/backend/tests/test_stream.py::TestSimpleEventBus::test_emit_prunes_failed_subscribers web/backend/tests/test_stream.py` -> 9 passed |
| 2026-04-22 | Feedback A-5/A-6/A-7 — usage convergence and sidecar determinism | Routed opencode audit usage through the shared `_usage_payload(...)` path, preserving opencode runtime metadata while adding finish/error/provider metadata fields and local provider cost estimates with rate-card versions. Normalized narrator sidecar exits through `SidecarOutcome` and one finalization path for skipped, runner unavailable, fallback, and completed outcomes. Gated `narrator_pre_step` emission to the first retry attempt so one logical broken step produces one pre-step narrator trigger. Evidence: `uv run pytest -q tests/test_opencode_audit.py` -> 10 passed; `uv run pytest -q tests/test_narrator_sidecar.py` -> 8 passed; `uv run pytest -q tests/test_engine.py::TestRunPhaseRetries::test_missing_pre_step_packet_emits_once_across_retries tests/test_engine.py::TestLoopEvents::test_missing_pre_step_packet_triggers_narrator_sidecar` -> 2 passed; full suite `uv run pytest -q` -> 539 passed, 2 warnings |
| 2026-04-22 | Feedback A-4 — lifecycle events through bridge | Routed PWA runner lifecycle events through `BridgingEventBus` instead of direct `SimpleEventBus`: `run_started`, `run_complete`, and `run_error` now land in the engine event bus and mirror to SSE. Added a shared memory error event helper so memory route failures use the same memory event/provenance shape as status/refresh/search. Added `web/backend/tests/test_runner.py::test_bridge_event_bus_receives_run_lifecycle_events` and `web/backend/tests/test_memory.py::test_memory_error_route_uses_memory_event_shape`. `uv run pytest -q web/backend/tests/test_runner.py::test_bridge_event_bus_receives_run_lifecycle_events web/backend/tests/test_runner.py web/backend/tests/test_memory.py::test_memory_error_route_uses_memory_event_shape web/backend/tests/test_memory.py` -> 11 passed. `uv run pytest -q web/backend/tests tests/test_events.py` -> 120 passed. Full suite `uv run pytest -q` -> 533 passed, 2 warnings |
| 2026-04-22 | Feedback A-1 — session runner resolver extraction | Extracted opencode/dry-run role runner resolution from `orchestrator/engine.py` into `orchestrator/lib/session_runners.py`, replacing the engine's branch chain with a table-backed helper while preserving `_session_runner_for(...)` as the engine-facing API. `engine.py` dropped from 708 to 686 lines. Added `tests/test_session_runners.py` for factory table coverage, injected runner precedence, dry-run auto-build/cache, and non-dry-run `None`. `uv run pytest -q tests/test_session_runners.py tests/test_dry_run.py tests/test_engine.py::TestLoopEvents tests/test_integration_dry_run.py` -> 19 passed. Full suite `uv run pytest -q` -> 531 passed, 2 warnings |
| 2026-04-22 | Feedback A-2/A-3 — event registry and packet contract hardening | Added `orchestrator/lib/event_types.py` as the backend durable event registry and wired `event_schema.validate_event` to reject unknown event types. Added frontend mirror `web/frontend/src/game/eventTypes.ts`, typed `RunEvent.type` against it, and made `EventEngine` ignore unknown fold inputs. Added `tests/test_event_schema.py` for unknown-type rejection and backend/frontend registry parity. A-3 closed by the B-2 canonical `emit_next_step_packet_event` helper, now used by engine, intent refresh, and narrator sidecar. `uv run pytest -q tests/test_event_schema.py web/backend/tests/test_stream.py web/backend/tests/test_events.py tests/test_events.py` -> 23 passed. `uv run pytest -q web/frontend/tests/test_event_engine.py::test_unknown_event_type_is_ignored_by_fold web/frontend/tests/test_event_engine.py` -> 10 passed. `./web/frontend/build.sh` -> built `dist/app.js` 1,310,721 bytes. Full suite `uv run pytest -q` -> 527 passed, 2 warnings |
| 2026-04-22 | Feedback B-1 — duplicate event return contract | Changed `SimpleEventBus.emit` so duplicate event ids return the existing stored event instead of a bare `None`, preserving dedupe while keeping callers' `dict` contract. Added `web/backend/tests/test_events.py::test_simple_event_bus_duplicate_emit_returns_existing_event`, which proves duplicate emits return the first event and do not append another replay item. `uv run pytest -q web/backend/tests/test_events.py::test_simple_event_bus_duplicate_emit_returns_existing_event web/backend/tests/test_events.py` -> 5 passed. Full suite `uv run pytest -q` -> 524 passed, 2 warnings |
| 2026-04-22 | Feedback B-3 — dead narrative digest helper removal | Deleted unused `Orchestrator._emit_narrative_digest` and its now-unused import. Exact grep for the private helper returned empty. `uv run pytest -q tests/test_engine.py::TestLoopEvents tests/test_narrative_digest.py tests/test_narrator_sidecar.py` -> 16 passed |
| 2026-04-22 | Feedback B-2 — canonical next-step packet emitter | Added `orchestrator/lib/next_step.py::emit_next_step_packet_event(...)` as the single backend/orchestrator event emitter for `next_step_packet_created`. Updated `Orchestrator._emit_next_step_packet`, intent packet refresh, and narrator sidecar refinement to call the shared helper. Removed the duplicate sidecar emitter and added `tests/test_next_step.py::test_emit_next_step_packet_event_uses_canonical_shape` for project id, packet identity, memory refs, artifact refs, and narrator refinement metadata. Grep now shows the only source emitter in `orchestrator/lib/next_step.py`; remaining hits are consumers/tests. `uv run pytest -q tests/test_next_step.py::test_emit_next_step_packet_event_uses_canonical_shape tests/test_next_step.py web/backend/tests/test_intents.py tests/test_narrator_sidecar.py tests/test_engine.py::TestLoopEvents` -> 22 passed. Full suite `uv run pytest -q` -> 523 passed, 2 warnings |
| 2026-04-22 | Feedback B-5 — dry-run narrator counter isolation | Changed `DryRunNarratorRunner` from class-level `_counter` mutation to per-instance counter state. Added `tests/test_narrator.py::test_dry_run_narrator_session_counter_is_instance_local`, proving one runner can advance to session 2 while a fresh runner still starts at session 1. `uv run pytest -q tests/test_narrator.py::test_dry_run_narrator_session_counter_is_instance_local tests/test_narrator.py::test_dry_run_narrator_runner_returns_valid_json` -> 2 passed. `uv run pytest -q tests/test_narrator.py tests/test_narrator_sidecar.py` -> 12 passed |
| 2026-04-22 | Feedback B-6 — persona synthesis ownership | Moved founding-team artifact writes behind public `PersonaForum.synthesize(...)` / `synthesize_without_compaction(...)` methods. `execute()` now delegates to the same role-owned path, while the HTTP control route calls the public no-compaction method and no longer invokes private helpers or writes `FOUNDING_TEAM.json` / `PERSONA_FORUM.md` inline. Extended `web/backend/tests/test_control.py::test_persona_synthesis_route_updates_workflow` to count artifact writes and prove each founding-team artifact is written once while still updating workflow and discussion events. `uv run pytest -q web/backend/tests/test_control.py::test_persona_synthesis_route_updates_workflow` -> 1 passed. `uv run pytest -q web/backend/tests/test_control.py orchestrator/tests/test_workflow.py tests/test_discussion.py` -> 19 passed |
| 2026-04-22 | Feedback B-7 — atomic run registration | Reworked `web/backend/runner.py::RunRegistry` from active-check-plus-register to atomic slot reservation: `try_register(project_id)` reserves a project while setup is still preparing files, `attach_thread(...)` binds the worker thread, and setup failures unregister the reservation. `start_run()` now reserves before resolving/writing the project directory, closing the concurrent double-start leak. Added `web/backend/tests/test_runner.py::test_start_run_concurrent_calls_schedule_one_thread`, which slows project resolution and fires 12 simultaneous starts; exactly one returns `True` and exactly one worker runs. `uv run pytest -q web/backend/tests/test_runner.py::test_start_run_concurrent_calls_schedule_one_thread` -> 1 passed. `uv run pytest -q web/backend/tests/test_runner.py web/backend/tests/test_projects.py web/backend/tests/test_control.py` -> 28 passed |
| 2026-04-22 | Feedback B-8 — narrator sidecar state locking | Hardened `orchestrator/lib/narrator_sidecar.py::record_sidecar_trigger` so sidecar debounce state updates take an exclusive POSIX file lock, reload state while holding the lock, and persist through same-directory temp file plus `os.replace`. Added `tests/test_narrator_sidecar.py::test_record_sidecar_trigger_preserves_concurrent_event_ids`, which records 40 trigger events concurrently and asserts no processed event ids are lost, the lock file exists, and temp files are cleaned up. `uv run pytest -q tests/test_narrator_sidecar.py::test_record_sidecar_trigger_preserves_concurrent_event_ids` -> 1 passed. `uv run pytest -q tests/test_narrator_sidecar.py tests/test_narrator.py tests/test_discussion.py tests/test_narrative_digest.py` -> 18 passed |
| 2026-04-22 | Feedback B-9/B-10 — truth-surface correction | Rewrote `docs/architecture.md` around the current opencode `SessionRunner` runtime, dry-run role runners, event/replay model, narrator sidecar, intents, next-step packets, narrative digests, discussion logs, memory provenance, and PWA lenses. Rewrote `docs/getting-started.md` so the first code block is a copy-runnable dry-run smoke script. Replaced the stale `STATUS.md` headline with the current follow/review/intervene state and archived the old H-series board behind the live M-series roadmap. Evidence: the docs stale-API grep returned empty; the STATUS stale-token grep returned empty; extracted first Getting Started code block and ran it with `.venv/bin/python` -> `dry-run smoke passed`; `git diff --check -- docs/architecture.md docs/getting-started.md project-management/STATUS.md analysis/04-22-2026-FEEDBACK.md` clean |
| 2026-04-22 | M06C — Goal Core body and project brief sheet | Added a backend-synthesized project brief endpoint, `GET /api/projects/{project_id}/brief`, so the frontend does not scrape raw project files. The response includes project title/summary, founding-team personas from `FOUNDING_TEAM.json`, a forum excerpt from `PERSONA_FORUM.md`, scrubbed text, and source refs. `Map.ts` loads the brief beside workflow data and passes it into `GameShell`. `SceneManager` now renders a tappable low-frequency `goal-core:body` in the existing Three.js scene and exposes `focusGoalCore()`. `GameShell` adds the `Goal` HUD control and `goal-core-sheet`, showing the project brief, founding team cards, forum pulse, source refs, current progress, latest story, next-step relation, and Nudge action. WebGL fallback renders the same Goal Core brief as an accessible block. Updated M06 checklist and frontend/backend guards. `uv run pytest -q web/backend/tests/test_projects.py web/frontend/tests/test_game_map.py web/frontend/tests/test_event_engine.py` → 34 passed. `uv run pytest -q tests/test_narrator_sidecar.py tests/test_narrator.py tests/test_discussion.py tests/test_narrative_digest.py tests/test_opencode_config.py tests/test_cli_run.py tests/test_opencode_audit.py web/backend/tests/test_control.py web/backend/tests/test_intents.py web/backend/tests/test_projects.py orchestrator/tests/test_workflow.py tests/test_engine.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 110 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,309,596 bytes). `git diff --check` clean |
| 2026-04-22 | M06B — Story Lens first pass | Added the second M06 lens slice in the PWA map. `GameShell` now mounts `story-lens-sheet` and a `Story: <count>` HUD lens control that opens a map-native story sheet over the existing Three.js scene. The sheet browses replay-folded narrative digests as `story-card` entries with highlights, risks, source refs, artifact refs, recent discussion rows/highlights, and the next slated step/nudge action. `SceneManager` now supports `SpatialViewLevel = "story"`, accepts `setStoryFocus(...)`, focuses the camera on atoms touched by recent digest/discussion events, and dims unrelated atoms through `AtomNode.setLensDimmed(...)`. Updated M06 checklist, handover, and bundle guards. `uv run pytest -q web/frontend/tests/test_game_map.py web/frontend/tests/test_event_engine.py` → 16 passed. `uv run pytest -q tests/test_narrator_sidecar.py tests/test_narrator.py tests/test_discussion.py tests/test_narrative_digest.py tests/test_opencode_config.py tests/test_cli_run.py tests/test_opencode_audit.py web/backend/tests/test_control.py web/backend/tests/test_intents.py orchestrator/tests/test_workflow.py tests/test_engine.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 92 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,297,608 bytes). `git diff --check` clean |
| 2026-04-22 | M06A — Object Lens map sheet | Added the first M06 lens slice inside `web/frontend/src/game/GameShell.ts`. Selecting an atom or the Object view now opens a map-native `object-lens-sheet` over the same Three.js scene, focuses the atom, and shows what the object is, its current state, consumed inputs/memory, produced artifact refs from replay-folded events, local story/discussion rows, next-step relation, queued/applied/ignored intents, and Shape/Nudge/Intercept actions wired to the existing compose dock. This is not a separate route or dashboard; it uses the existing scene, bottom HUD, and folded `EngineSnapshot`. WebGL fallback remains list-based, and narrator/next-step overlays hide cleanly around the object lens. Updated `project-management/srs-mini-phases/M06-pwa-lenses-and-map-surface.md` to mark the first object lens pass complete and refreshed `project-management/HANDOVER_M02B.md` for the next agent. `uv run pytest -q web/frontend/tests/test_game_map.py web/frontend/tests/test_event_engine.py` → 16 passed. `uv run pytest -q tests/test_narrator_sidecar.py tests/test_narrator.py tests/test_discussion.py tests/test_narrative_digest.py tests/test_opencode_config.py tests/test_cli_run.py tests/test_opencode_audit.py web/backend/tests/test_control.py web/backend/tests/test_intents.py orchestrator/tests/test_workflow.py tests/test_engine.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 92 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,286,675 bytes). `git diff --check` clean |
| 2026-04-22 | M05D — narrator polish and workflow phase support | Completed the remaining M05 narrator plumbing. `orchestrator/roles/narrator.py` now persists rejected/empty narrator output diagnostics under `.orchestrator/narrative/diagnostics/*.json` and returns those artifact refs on fallback. `orchestrator/lib/narrator_sidecar.py` includes those diagnostics on `narrator_sidecar_fallback`, accepts `narrator_pre_step` and `narrator_phase_requested` triggers, and keeps invalid output from producing refined packets. `orchestrator/engine.py` emits a literal pre-step narrator trigger when no next-step packet can be built, supports optional workflow phases with handler `narrator`, and avoids double sidecar execution on the narrator phase's own `phase_done`. `orchestrator/lib/workflow.py` now accepts `narrator` as a phase handler. Added tests for diagnostic artifact persistence, fallback artifact refs, pre-step trigger, optional narrator workflow phase, and workflow validation. `uv run pytest -q tests/test_narrator.py tests/test_narrator_sidecar.py tests/test_engine.py::TestLoopEvents orchestrator/tests/test_workflow.py` → 28 passed. `uv run pytest -q tests/test_narrator_sidecar.py tests/test_narrator.py tests/test_discussion.py tests/test_narrative_digest.py tests/test_opencode_config.py tests/test_cli_run.py tests/test_opencode_audit.py web/backend/tests/test_control.py web/backend/tests/test_intents.py orchestrator/tests/test_workflow.py tests/test_engine.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 92 passed. `uv run pytest -q web/frontend/tests/test_event_engine.py web/frontend/tests/test_game_map.py` → 16 passed. `uv run python -m py_compile orchestrator/lib/narrator_sidecar.py orchestrator/roles/narrator.py orchestrator/engine.py orchestrator/lib/workflow.py` → passed |
| 2026-04-22 | M05C — narrator sidecar hook + packet refinement | Added `orchestrator/lib/narrator_sidecar.py` as the debounce/coordinator layer for narrator sidecar work. Engine phase/gate boundaries now route `phase_done`, `gate_decided`, and `phase_error` through the sidecar; when a narrator runner exists it emits a validated narrator digest and then a narrator-refined `next_step_packet_created` with `refined_by="narrator"`, `base_packet_id`, `narrative_digest_id`, and source-backed context preview. If no runner or invalid output exists, deterministic M04 digest emission remains the fallback and a `narrator_sidecar_skipped` / `narrator_sidecar_fallback` diagnostic event is emitted. `POST /intents` now emits a fast non-blocking `narrator_sidecar_requested` event after the refreshed authoritative packet, preserving immediate operator feedback. `SimpleEventBus.emit()` now returns the normalized event so routes can source-link follow-up events. Added `tests/test_narrator_sidecar.py` plus engine/backend/integration assertions for refined packets, debounce, fallback, and sidecar request source refs. `uv run pytest -q tests/test_narrator_sidecar.py tests/test_narrator.py tests/test_engine.py::TestLoopEvents web/backend/tests/test_intents.py` → 19 passed. `uv run pytest -q tests/test_narrator_sidecar.py tests/test_narrator.py tests/test_discussion.py tests/test_narrative_digest.py tests/test_opencode_config.py tests/test_cli_run.py tests/test_opencode_audit.py web/backend/tests/test_control.py web/backend/tests/test_intents.py orchestrator/tests/test_workflow.py tests/test_engine.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 89 passed. `uv run pytest -q web/frontend/tests/test_event_engine.py web/frontend/tests/test_game_map.py` → 16 passed. `uv run python -m py_compile orchestrator/lib/narrator_sidecar.py orchestrator/engine.py web/backend/routes/intents.py web/backend/routes/stream.py` → passed |
| 2026-04-22 | M05B — narrator dialogue presentation surface | Updated `docs/pwa-narrative-replay-and-intervention-design.md`, `project-management/srs-mini-phases/M05-narrator-scribe-agent.md`, and `project-management/srs-mini-phases/M06-pwa-lenses-and-map-surface.md` so narrator output is explicitly neutral project synthesis rendered in a visually JRPG-inspired dialogue shell, not a dashboard card or a JRPG roleplay prompt; documented independent `routing.narrator` model selection for OpenRouter/local/on-prem mixes. Added `GameShell` narrator dialogue overlay (`data-role="narrator-dialogue"`) with speaker frame, serif text, typewriter reveal, skip/replay controls, source affordance, live/replay label, reduced-motion fallback, and accessible fallback rendering. The overlay derives from replay-folded `latestDigest` or recent discussion and hides behind active intervention sheets/docks. `uv run pytest -q web/frontend/tests/test_game_map.py web/frontend/tests/test_event_engine.py` → 16 passed. `uv run pytest -q tests/test_narrator.py tests/test_discussion.py tests/test_narrative_digest.py tests/test_opencode_config.py tests/test_cli_run.py tests/test_opencode_audit.py web/backend/tests/test_control.py web/backend/tests/test_intents.py orchestrator/tests/test_workflow.py tests/test_engine.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 84 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,278,856 bytes). Playwright CLI screenshots captured: `/tmp/m05b-narrator-desktop.png` (1440×900) and `/tmp/m05b-narrator-mobile.png` (393×852) |
| 2026-04-22 | M05A — narrator role contract and read-only opencode path | Added `orchestrator/roles/narrator.py`, `orchestrator/roles/narrator_dryrun.py`, and `orchestrator/roles/prompts/narrator/generate.txt`. The narrator now builds a bounded scrubbed packet from project goal, workflow, recent events, next-step packet, discussion rows, and small artifact excerpts; validates JSON output against packet event ids and safe artifact refs; emits validated `narrative_digest_created` plus `discussion_entry_created`; audits opencode sessions through `emit_session_audit`; and falls back to deterministic M04 digest without ledger corruption on invalid output. `opencode_config` now declares a read/grep-only `narrator` agent, and the live runner builder can route `narrator`. `uv run pytest -q tests/test_narrator.py tests/test_opencode_config.py::test_narrator_agent_is_read_only tests/test_cli_run.py::test_live_runner_builder_includes_narrator` → 8 passed. `uv run python -m py_compile orchestrator/roles/narrator.py orchestrator/roles/narrator_dryrun.py` → passed. `uv run pytest -q tests/test_narrator.py tests/test_opencode_config.py tests/test_cli_run.py tests/test_opencode_audit.py` → 29 passed. `uv run pytest -q tests/test_narrator.py tests/test_discussion.py tests/test_narrative_digest.py tests/test_opencode_config.py tests/test_cli_run.py tests/test_opencode_audit.py web/backend/tests/test_control.py web/backend/tests/test_intents.py orchestrator/tests/test_workflow.py tests/test_engine.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 84 passed. `uv run pytest -q web/frontend/tests/test_event_engine.py web/frontend/tests/test_game_map.py` → 16 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,271,126 bytes). `git diff --check` clean |
| 2026-04-22 | M04B — durable discussion log projection | Added `orchestrator/lib/discussion.py` with scrubbed `discussion_entry_created` / `discussion_highlight_created` projection helpers, `.orchestrator/discussion/log.jsonl`, `.orchestrator/discussion/highlights.jsonl`, and rebuild support via `python -m orchestrator.lib.discussion --rebuild`. Operator control instructions and map `note` intents now create discussion entries; persona synthesis and `PersonaForum.execute()` project persona statements into the same log with a sourced highlight. EventEngine folds replayable `discussion` and `discussionHighlights`; GameShell renders discussion rows in the map Next-Step Sheet/fallback surface. `uv run pytest -q tests/test_discussion.py web/backend/tests/test_control.py::test_control_instruction_route_persists_operator_note web/backend/tests/test_control.py::test_persona_synthesis_route_updates_workflow web/backend/tests/test_intents.py::test_note_intent_creates_discussion_entry` → 7 passed. `uv run pytest -q web/frontend/tests/test_event_engine.py::test_discussion_replay_fold web/frontend/tests/test_game_map.py::test_game_shell_bundle_contains_replay_surface` → 2 passed. `uv run pytest -q tests/test_discussion.py tests/test_narrative_digest.py web/backend/tests/test_control.py web/backend/tests/test_intents.py orchestrator/tests/test_workflow.py tests/test_engine.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 55 passed. `uv run pytest -q web/frontend/tests/test_event_engine.py web/frontend/tests/test_game_map.py` → 16 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,271,126 bytes). `git diff --check` clean |
| 2026-04-22 | M04A — deterministic narrative digest spine | Added `orchestrator/lib/narrative.py` with deterministic DWA generation, source validation, secret scrubbing, and `.orchestrator/narrative/digests/*.json`/`.md` artifact writes. Engine now emits `narrative_digest_created` after completed phase/gate boundaries when the event bus returns durable event ids. EventEngine folds replayable `digests`/`latestDigest`, and GameShell renders the latest story inside the map Next-Step Sheet/fallback surface. `uv run pytest -q tests/test_narrative_digest.py tests/test_engine.py::TestLoopEvents::test_phase_completion_emits_narrative_digest` → 4 passed. `uv run pytest -q web/frontend/tests/test_event_engine.py::test_narrative_digest_replay_fold web/frontend/tests/test_game_map.py::test_game_shell_bundle_contains_replay_surface` → 2 passed. `uv run pytest -q tests/test_narrative_digest.py tests/test_engine.py tests/test_next_step.py tests/test_intents.py web/backend/tests/test_intents.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 48 passed. `uv run pytest -q web/frontend/tests/test_event_engine.py web/frontend/tests/test_game_map.py` → 15 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,266,108 bytes). `git diff --check` clean |
| 2026-04-22 | M02E — 3D map intent packet visuals | SceneManager now renders persistent intent packet sprites above target atoms from folded `pendingIntents`, `appliedIntents`, and `ignoredIntents`; queued/applied/ignored states use distinct billboard glyphs/colors and remain event-ledger derived. `uv run pytest -q web/frontend/tests/test_game_map.py` → 7 passed. `uv run pytest -q web/frontend/tests/test_game_map.py web/frontend/tests/test_event_engine.py` → 14 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,261,772 bytes). Playwright desktop and mobile 393x852 checks showed no sheet/HUD overlap, sheet within viewport, and nonblank canvas signals (`103408`, `107370`). `git diff --check` clean |
| 2026-04-22 | M02D — PWA-side client intent ids | GameShell now generates a `client_intent_id` before every map compose submission and includes it in the `POST /intents` body for `interject`, `intercept`, and `reroute` actions. `uv run pytest -q web/frontend/tests/test_game_map.py` → 7 passed. `uv run pytest -q web/frontend/tests/test_game_map.py web/frontend/tests/test_event_engine.py web/backend/tests/test_intents.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 17 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,256,779 bytes). `git diff --check` clean |
| 2026-04-22 | M02C — packet refresh after operator intent | `POST /api/projects/{project_id}/intents` now emits `operator_intent` and then a refreshed authoritative `next_step_packet_created` using the current project state/current phase when present. Backend tests assert event order and queued intent attachment; the end-to-end rehearsal now proves packet refresh after each submitted intent before engine execution continues. `uv run pytest -q web/backend/tests/test_intents.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 3 passed. `uv run pytest -q tests/test_next_step.py tests/test_intents.py web/backend/tests/test_intents.py web/frontend/tests/test_event_engine.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 22 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,256,262 bytes). `git diff --check` clean |
| 2026-04-22 | M02B handover | Added `project-management/HANDOVER_M02B.md`, a contextless pickup file covering the lifecycle model, landed slices, verification commands, worktree cautions, invariants, and recommended next slices after M02B. Docs-only change; `git diff --check -- project-management/HANDOVER_M02B.md` clean |
| 2026-04-22 | M02B Step 7 — end-to-end intent rehearsal | Added `tests/test_integration_m02b_intent_lifecycle_rehearsal.py`, covering backend project intent submission, durable queued registry rows, engine next-step packet emission, stale targeted intent terminal ignore, global intent retention for the following packet, and EventEngine replay folding from the combined ledger. `uv run pytest -q tests/test_next_step.py tests/test_intents.py web/backend/tests/test_intents.py web/frontend/tests/test_event_engine.py tests/test_integration_m02b_intent_lifecycle_rehearsal.py` → 21 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,256,262 bytes). `git diff --check` clean |
| 2026-04-22 | M02B Step 6 — PWA Next-Step Sheet | GameShell now has an always-present bottom HUD `Next:` control driven by `snapshot.nextStepPacket`, opening a compact `next-step-sheet` over the map. The sheet shows objective, why-now, context preview, inputs, memory refs, source refs, queued/applied/ignored intents, and a `Nudge` action wired into the existing compose dock for the next step. `uv run pytest -q web/frontend/tests/test_game_map.py web/frontend/tests/test_event_engine.py` → 14 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,256,262 bytes). Playwright desktop and mobile 393x852 checks showed no sheet/HUD overlap, sheet within viewport, and nonblank canvas signals (`100312`, `96573`). `git diff --check` clean |
| 2026-04-22 | M02B Step 5 — frontend intent lifecycle fold | EventEngine now exposes replayable `pendingIntents`, `appliedIntents`, and `ignoredIntents` maps from `operator_intent`, `operator_intent_applied`, and `operator_intent_ignored` events. Current next-step packets merge in relevant pending intents submitted after packet creation, so the PWA can answer what was submitted and what happened to it at each replay index. `uv run pytest -q web/frontend/tests/test_event_engine.py` → 7 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,245,195 bytes). `git diff --check` clean |
| 2026-04-22 | M02B Step 4 — ignored intent outcomes | Added idempotent ignored-intent terminal transitions. Successful phase/gate completion now marks stale targeted queued intents `ignored` with `target_step_passed`, and unsupported prompt kinds with `unsupported_kind`, emitting `operator_intent_ignored`. `uv run pytest -q tests/test_intents.py tests/test_engine.py` → 36 passed. `uv run pytest -q tests/test_coder.py tests/test_reviewer.py tests/test_tester.py tests/test_deployer.py tests/test_architect_judge.py web/frontend/tests/test_event_engine.py` → 99 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,240,514 bytes). `git diff --check` clean |
| 2026-04-22 | M02B Step 3 — prompt intent application | Added idempotent prompt intent consumption: queued `interject`/`intercept`/`reroute` intents are injected into operator context once, registry rows become `applied`, and `operator_intent_applied` fires when role prompt augmentation has an event bus. Opencode-backed roles now pass `state.events` into `append_operator_context`. `uv run pytest -q tests/test_intents.py tests/test_coder.py tests/test_reviewer.py tests/test_tester.py tests/test_deployer.py tests/test_architect_judge.py` → 99 passed. `uv run pytest -q web/backend/tests/test_intents.py tests/test_next_step.py web/frontend/tests/test_event_engine.py` → 11 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,240,514 bytes). `git diff --check` clean |
| 2026-04-22 | M02B Steps 1-2 — durable queued intents on next-step packets | Added `orchestrator/lib/intents.py` with `.orchestrator/control/intents.jsonl`, backend-generated/returned `client_intent_id`, scrubbed queued intent persistence, and `queued_intents` attached to deterministic next-step packets. EventEngine now exposes `nextStepPacket.queuedIntents` in live/replay snapshots. `uv run pytest -q web/backend/tests/test_intents.py tests/test_intents.py tests/test_next_step.py web/frontend/tests/test_event_engine.py` → 13 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,240,514 bytes). `git diff --check` clean |
| 2026-04-22 | M02B intent lifecycle execution plan | Added `project-management/srs-mini-phases/M02B-intent-lifecycle-execution-plan.md` after inspecting `web/backend/routes/intents.py`, `orchestrator/lib/control.py`, `orchestrator/lib/next_step.py`, `EventEngine`, and `GameShell`. The plan defines the sequential bridge from `operator_intent` → durable intent registry → next-step packet attachment → prompt application/ignore outcomes → PWA lifecycle fold → Next-Step Sheet, with quality gates after each slice. Docs-only change; `git diff --check` clean |
| 2026-04-22 | M02a deterministic next-step packet slice | Added `orchestrator/lib/next_step.py` with deterministic workflow selection and M08 `memory_provenance()` in packet inputs/source refs. Engine now emits `next_step_packet_created` before phase start and after phase/gate approval. EventEngine folds `nextStepPacket` and replay scrub shows the packet known at that event index. `uv run pytest -q tests/test_next_step.py tests/test_engine.py web/frontend/tests/test_event_engine.py` → 37 passed. `uv run pytest -q tests/test_next_step.py web/backend/tests/test_intents.py web/backend/tests/test_memory.py tests/test_memory_palace.py` → 11 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,240,268 bytes). `git diff --check` clean |
| 2026-04-22 | M08a memory provenance packet + search scrubbing | Added `memory_provenance()` as a compact packet for future next-step/DWA citation; memory search now scrubs query/room before CLI invocation and scrubs output before response/cache, keeping `last-search.txt` clean. `uv run pytest -q tests/test_memory_palace.py web/backend/tests/test_memory.py tests/test_opencode_audit.py orchestrator/tests/test_workflow.py` → 23 passed. `uv run pytest -q web/backend/tests/test_intents.py tests/test_usage_events.py` → 3 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,238,657 bytes). `git diff --check` clean |
| 2026-04-22 | M08a Memory Provenance Packet partial | Added stable MemPalace wake-up hashing (`wake_up_hash`), memory status/refreshed/search/error event emission, opencode audit memory provenance (`runtime.memory.wake_up_hash`), PWA EventEngine folding of memory hash/refs, and legacy workflow migration guard to keep `memory_refresh` before planning. `uv run pytest -q orchestrator/tests/test_workflow.py web/backend/tests/test_memory.py web/backend/tests/test_control.py tests/test_opencode_audit.py` → 25 passed. `uv run pytest -q tests/test_engine.py tests/test_loop_refs.py orchestrator/tests/test_workflow.py` → 46 passed. `uv run pytest -q web/backend/tests/test_events.py web/backend/tests/test_memory.py` → 7 passed. `./web/frontend/build.sh` → passed (`dist/app.js` 1,238,657 bytes). `git diff --check` clean |
| 2026-04-22 | M08 memory/opencode plan incorporation | Updated `project-management/srs-mini-phases/M08-memory-spine-and-mempalace.md` with the two-channel MemPalace→opencode architecture, M08a Memory Provenance Packet, M08b optional MemPalace MCP for selected opencode agents, corrected implementation paths, and explicit quality gates. Updated the mini-phase README first-delivery sequence and execution checklist guardrails. Docs-only change; `git diff --check` clean |
| 2026-04-21 | Poly Pizza display wiring | Frontend build passed; asset/game/backend smoke tests passed; GLB requests returned `200 model/gltf-binary`; screenshot `/tmp/council-map-poly-pizza-props.png` |
| 2026-04-21 | Ultimate Space Kit inventory | `jq` manifest validation passed; all 87 GLBs have `glTF` magic headers; `git diff --check` passed |
| 2026-04-21 | Phase 1 flow foundation + view levels | `uv run pytest -q web/frontend/tests/test_game_map.py` passed; `./web/frontend/build.sh` passed; mobile HUD now exposes Run/Loop/Object scales |
| 2026-04-21 | Phase 0 Task 0.1 — verification commands | `uv run pytest -q web/backend/tests/test_pwa_serving.py web/backend/tests/test_events.py` 10 passed; `uv run pytest -q web/frontend/tests/test_asset_manager.py web/frontend/tests/test_game_map.py` 9 passed; `./web/frontend/build.sh` built 1,237,754-byte `dist/app.js`; `git diff --check` clean |
| 2026-04-21 | Honest reset | Harness H0/H1/H2 phases inserted ahead of visual-map work. Verified firsthand: `runner.py:160` hardcodes auto-approve; two `GateRegistry` classes (`orchestrator/lib/gates.py:34`, `web/backend/routes/gates.py:60`); `logger.py:138` discards `messages`/`response` params |
| 2026-04-21 | Harness 0 Task H0.1 — raw I/O writer | New `orchestrator/lib/raw_io.py` + shared `orchestrator/lib/scrub.py` (also consumed by `web/backend/routes/intents.py`). `uv run pytest -q tests/test_raw_io.py web/backend/tests/test_intents.py` → 5 passed. Full suite `uv run pytest -q` → 494 passed, 1 skipped. `git diff --check` clean |
| 2026-04-21 | Harness 0 Task H0.2 — wire log_llm_call | `setup_phase_logger` now stashes `project_dir` on the logger; `log_llm_call` generates a `request_id`, writes the token summary to `<phase>.jsonl`, and appends the full round-trip (messages + response_content + reasoning_content + usage + endpoint) to `raw-io.jsonl`. Call sites (`roles/base.py`, `roles/architect.py` ×3) pass `endpoint=getattr(self.llm, "base_url", None)`. Full suite → 496 passed, 1 skipped. `git diff --check` clean |
| 2026-04-21 | Harness 0 Task H0.3 — end-to-end capture guard | New `tests/test_integration_raw_io.py` seeds `qwendea.md` and the fake LLM's reasoning with unique sentinels, runs the full dry-run pipeline, and asserts both sentinels, ≥1 known role, and unique `request_id`s in `raw-io.jsonl`. Full suite → 497 passed, 1 skipped. `git diff --check` clean. **Phase H0 complete.** |
| 2026-04-21 | Harness 1 Task H1.1 — consolidate GateRegistry | Deleted the shadow `GateRegistry` in `web/backend/routes/gates.py`. Canonical `orchestrator/lib/gates.GateRegistry` now carries `open_gate`, `list_gates`, `get_gate`, idempotent `decide(name, decision, reason)`, plus a new `GateRegistryStore` for multi-project HTTP traffic. Web routes become a thin facade. `rg "^class GateRegistry" → 1` match. `uv run pytest -q tests/test_gates.py web/backend/tests/test_gates.py` → 30 passed. Full suite → 500 passed, 1 skipped. `git diff --check` clean |
| 2026-04-21 | Harness 3 — live-pool routing guardrail | H3.1 rewrote `tests/test_legacy pool module` with 8 async tests that actually `await pool.chat`: preferred-endpoint routing, fallback on `ConnectionError` (with healthy flag flip), `NoHealthyEndpoint` on exhaustion, unknown-role raise, lock serialization within one endpoint (two concurrent calls delayed ≥ 2 × delay), parallelism across endpoints (< 1.8 × delay), health_check flag flip + recovery. H3.2 new `orchestrator/lib/pool_log.py` persists every routing decision to `.orchestrator/logs/pool.jsonl` with `call_id, role, chosen_endpoint, attempted_endpoints, fell_back, reason`; `UpstreamPool.chat` gained `project_dir` + `call_id` kwargs. H3.3 `tests/test_integration_h3_pool_observability.py` runs two `pool.chat` calls via `asyncio.gather` and asserts distinct endpoint labels land in `pool.jsonl`; a second test asserts `fell_back=True` when primary raises `ConnectionError`. Uncovered a critical gap en route: `UpstreamPool.chat` is `async def` but roles call `self.llm.chat` synchronously → a real live run would fail on first call. Filed as new phase **H5 Pool Live Wiring**, now blocking H2.2. Full suite → 529 passed, 1 skipped. **Phase H3 complete.** |
| 2026-04-21 | Harness 4 — cost/usage roundtrip guardrail | H4.0 unified the join key: every emit/log pair site in `roles/base.py` (_chat), `roles/architect.py` (×3 try blocks: draft, refine, judge), generates one `call_id = new_event_id()` and threads it to both `emit_llm_usage(call_id=...)` and `log_llm_call(request_id=...)`. H4.1 new `orchestrator/lib/usage_writer.py` persists every `usage_recorded` payload to `.orchestrator/logs/usage.jsonl`; hooks added to `emit_llm_usage` and `emit_advisor_usage` so the on-disk copy can't be forgotten. H4.2 new `orchestrator/lib/usage_summary.py:rollup(project_dir)` joins raw-io.jsonl + usage.jsonl on the shared id, returns `RunSummary` with `per_role`, `per_phase`, `totals`, and `orphans` (calls that fell out of one file). `orchestrator inspect <project> --costs` prints the rollup. H4.3 `tests/test_integration_h4_usage_roundtrip.py` runs the full dry-run pipeline and asserts every raw-io `request_id` has a matching usage `call_id`, rollup totals equal the underlying sums, and cost totals match the per-record effective costs. Full suite → 519 passed, 1 skipped. **Phase H4 complete.** |
| 2026-04-21 | Harness 2 Task H2.1 — programmatic gated dry-run rehearsal | New `tests/test_integration_h2_1_rehearsal.py` drives the full PWA-gated code path: starts `_run_orchestrator` on a daemon thread with `require_human_approval=True`, approves every pending gate via `POST /api/projects/{id}/gates/{name}/decide`, and asserts run_complete + all expected roles captured in `raw-io.jsonl` + unique request_ids. `uv run pytest -q tests/test_integration_h2_1_rehearsal.py` → 1 passed. Full suite → 510 passed, 1 skipped. Out-of-test rehearsal into `/tmp/guildr-rehearsal/` produced 5 raw-io records (architect×2, coder, reviewer, deployer — tester shells out to pytest and makes no LLM call). `git diff --check` clean. Manual PWA walk-through + H2.2 live-path run against PRIMARY are the next steps |
| 2026-04-21 | Harness 1 Task H1.3 — per-run gate opt-in | `require_human_approval` is now plumbed through `web/backend/runner.py` (default False), the HTTP start route (`StartRequest.require_human_approval: bool = False`), the PWA start panel (new "Gate my approval at each phase" checkbox, default off), and the CLI (`--gate` flag, mutually exclusive with `--no-gates`). Engine Config no longer hardcoded in runner. New tests: `test_web_runner_defaults_to_idle_rpg_mode`, `test_web_runner_threads_gate_opt_in_into_config`, `test_start_project_defaults_to_idle_rpg_mode`, `test_start_project_honors_gate_opt_in`, `test_gate_flag_forces_require_human_approval_true`, `test_gate_and_no_gates_are_mutually_exclusive`. Full suite → 509 passed, 1 skipped. `./web/frontend/build.sh` → `dist/app.js` 1,238,255 bytes. `git diff --check` clean. **Phase H1 complete.** |
| 2026-04-21 | Phase 0 Task 0.2 — Ultimate Space Kit asset test guardrails | Added `tests/test_ultimate_space_kit_manifest.py` (4 tests: top-level schema incl. CC0 license + asset_count = len(assets); every manifest path resolves on disk after `urllib.parse.unquote`; summed per-asset `bytes` equals `total_bytes` and matches disk `stat`; `sw.js` precache contains no `ultimate-space-kit` / `/poly-pizza/` path). Extended `web/backend/tests/test_pwa_serving.py::test_ultimate_space_kit_glb_served_with_model_mime` asserting `Astronaut.glb` is served as `model/gltf-binary` with a `glTF` magic header. `uv run pytest -q tests/test_ultimate_space_kit_manifest.py web/backend/tests/test_pwa_serving.py` → 11 passed. Also promoted `project-management/AGENT_ONBOARDING.md` to a full fresh-context handover (read order, repo map, glossary, known traps incl. URL-encoded manifest paths + async/sync pool mismatch, blocked-item pickup playbook) |
| 2026-04-21 | Harness 5 Task H5.1 — sync facade for UpstreamPool | New `orchestrator/lib/sync_legacy pool module:SyncPoolClient` wraps the async pool with a sync `.chat(messages, *, call_id=None, **kw)` that `asyncio.run`s `pool.chat(role, messages, project_dir=..., call_id=..., **kw)` and updates `base_url` to the chosen endpoint label. `orchestrator/engine.py` gained `_llm_for(role)` and swapped all 6 role dispatch sites from `self._pool.chat` (a coroutine-returning bound method, unreachable from sync role code) to the facade. `LLMClient.chat` now accepts (and ignores) `call_id` so the same call site signature works for both fake and pool paths; `roles/base.py:_chat` + `roles/architect.py` ×3 thread `call_id=call_id` into `self.llm.chat` so `pool.jsonl` shares the H4 join key. New `tests/test_sync_legacy pool module` (5 tests) pins: sync return of materialized `LLMResponse`, `max_tokens` forwarding, `base_url` updated post-call, `call_id` lands verbatim in `pool.jsonl`, two consecutive calls don't leak the inner loop. `uv run pytest -q tests/test_sync_legacy pool module tests/test_legacy pool module tests/test_integration_h3_pool_observability.py tests/test_integration_h4_usage_roundtrip.py tests/test_integration_raw_io.py tests/test_integration_h2_1_rehearsal.py` → 18 passed. Full suite `uv run pytest -q` → 539 passed, 1 skipped. `git diff --check` clean. **H2.2 still blocked** — unblocks once H5.2 parses env URLs and constructs a pool in `runner.py`/`cli/run.py` |
| 2026-04-21 | Harness 6 Task H6.0 — opencode spike | Installed `opencode-ai@1.14.20` via npm. Smoke-tested `opencode run --format json --model primary/Qwen3.6-35B-A3B-UD-Q5_K_XL.gguf --dangerously-skip-permissions` against the user's PRIMARY (192.168.1.13) — streams NDJSON events (`step_start`, `text`, `tool_use`, `step_finish`) on stdout, exits on completion. Ran a tool-using session that wrote `/tmp/opencode-smoke.txt` and globbed `/tmp` — every tool call is a structured event with `tool`, `input`, `output`, `state.status`, `time.start/end`. `opencode export <sessionID>` dumps the full session JSON (info + messages + parts) covering every field our current audit trail persists (`model.{providerID,modelID}`, `tokens.{total,input,output,reasoning,cache}`, `cost`, `summary.{additions,deletions,files}`). Five H6 decisions locked (see `docs/research/opencode-runtime.md`): stdout-NDJSON streaming > HTTP `serve`; per-project config overlay > mutating `~/.config/opencode/`; one opencode agent per SDLC role with declarative tool allowlists; gates between-sessions only for v1; `opencode export` as the canonical audit record. Binary location: `/Users/karol/.nvm/versions/node/v22.21.0/bin/opencode` |
| 2026-04-21 | Harness 6 Task H6.1 — generate opencode.json from config.yaml | New `orchestrator/lib/opencode_config.py` (`build_opencode_config`, `opencode_config_path`, `write_opencode_config`) emits a valid opencode.json from any `EndpointsConfig`. Every endpoint becomes a `@ai-sdk/openai-compatible` provider; every distinct model reachable from either the endpoint default or a `RouteEntry.model` override is registered under that provider so `--model <endpoint>/<modelID>` resolves for any declared route. `api_key` and `headers` propagate into `provider.<name>.options`; same model id on two different endpoints gets disambiguated display names (`qwen:30b @ fast` / `qwen:30b @ slow`). Output lands at `<project_dir>/.orchestrator/opencode/opencode.json` — the user's global `~/.config/opencode/opencode.json` is never touched. New `tests/test_opencode_config.py` (7 tests): provider registration, `/v1` suffix, `apiKey` + `headers` propagation, every routing-reachable model registered, parent-dir creation + round-trip JSON, overwrite-on-regeneration, same-id-different-endpoint naming. Validated against the real binary: `opencode models localtest` (from the generated-config cwd) returned `localtest/<modelID>`. `uv run pytest -q` → 562 passed, 1 skipped (+7 from H6.1). `git diff --check` clean |
| 2026-04-21 | Harness 6 Task H6.3b — Tester role on opencode runtime | `orchestrator/roles/tester.py` rewritten to drop `BaseRole` and drive one opencode session per `execute()` pass. `__init__(runner: SessionRunner, state, phase_logger)`. Framework still extracts the shell-runnable evidence commands from each sprint-plan task (`_extract_commands` + `_command_validation_error` preserved as static helpers, covered by unit tests) and renders them into a task-block the prompt interpolates — the agent is never free to invent its own verification story. Agent runs each command via its bash tool and returns `TEST_REPORT.md` as its final assistant text; orchestrator writes it verbatim (parallel to deployer's DEPLOY.md handling). Three session-level failures surface as `TesterError`: runner exception, non-zero exit, empty assistant text. `TaskResult` dataclass + `verify_task` helper dropped — they presupposed the per-task deterministic path, and the session verifies every task in one pass. No-tasks short-circuit writes a stub `TEST_REPORT.md` without invoking the runner. Audit emits fire *before* any failure raise. New `orchestrator/roles/tester_dryrun.py:DryRunTesterRunner` returns a canned VERIFIED report matching the one-task dry-run sprint plan. `orchestrator/roles/prompts/tester/generate.txt` rewritten from "retained for documentation only" to a real agent prompt with the TEST_REPORT.md shape contract + rules (VERIFIED iff all commands exit 0; RERUN_FAILED on failure/skip/missing commands; MISMATCH only on output-vs-criteria contradiction). Engine `_tester` now resolves via `_session_runner_for("tester")` + auto-builds `DryRunTesterRunner` when `dry-run injection` is set. `cli/run.py`: opencode sessions now built for `("coder", "tester", "reviewer", "deployer")`; removed `_DRY_RUN_TESTER_REPORT` + "you are a qa engineer" branch from `_ContentAwareFake.chat`. Tests: `tests/test_tester.py` rewritten around `_FakeRunner` (covers command extraction incl. Run-backticks / git-diff / known-prefix / manual-skip / prose-skip, dev-server validation, task-block rendering incl. missing-command + dev-server flagging, execute writes report, prompt contains commands, no-tasks short-circuit, runner-exception wrapping, non-zero exit, empty-text rejection, audit on success + on failure) — the 2 pre-existing `python: command not found` failures are gone because execution is agent-driven now. `tests/test_cli_run.py::test_dry_run_llm_dispatches_by_role` drops the tester branch; `tests/test_integration_h5_two_endpoint_wireup.py` injects a `DryRunTesterRunner` and asserts `tester` is now absent from `pool.jsonl`; `tests/test_integration_h2_1_rehearsal.py` expanded `_EXPECTED_ROLES` to include tester (raw-io.jsonl now carries a tester row via `emit_session_audit`). Full suite → 558 passed, 1 skipped in 68s. `git diff --check` clean |
| 2026-04-21 | Harness 6 Task H6.3d — Deployer role on opencode runtime | `orchestrator/roles/deployer.py` rewritten to drop `BaseRole` and drive an opencode session per deployment pass. `__init__(runner: SessionRunner, state, phase_logger)`. `execute()` builds one combined prompt (detected deploy configs + detected env-var names + review verdict), calls `runner.run(prompt)`, emits audit via `emit_session_audit` *before* any failure raise, then writes `DEPLOY.md` from `result.assistant_text.strip()`. Three session-level failures surface as `DeployerError`: runner exception, non-zero exit, empty assistant text. New `orchestrator/roles/deployer_dryrun.py:DryRunDeployerRunner` returns a canned `DEPLOY.md` — engine auto-provides it when `dry-run injection` is set, mirroring the coder/reviewer dry-run path. `orchestrator/roles/prompts/deployer/generate.txt` merged into one prompt and notes that read/grep tools are available. Engine `_deployer` now resolves via `_session_runner_for("deployer")`. `cli/run.py` drops the `_DRY_RUN_DEPLOY_REPORT` branch and builds opencode sessions for `("coder", "reviewer", "deployer")` from endpoints routing; `web/backend/runner.py` inherits the change. Detection helpers (`_detect_deploy_configs`, `_detect_env_vars` — hidden/venv skip preserved, sorted + private-var filtered — and `_load_review`) unchanged. Tests: `tests/test_deployer.py` rewritten around a `_FakeRunner` (covers deploy-config detection, env-var detection incl. hidden/venv skip + sort + private-var filter, review loading, execute flow, prompt content, runner-exception wrapping, non-zero exit, empty-text rejection, audit emission on success + on failure, `get_required_env_vars`); `tests/test_integration_h5_two_endpoint_wireup.py` injects a `DryRunDeployerRunner` alongside coder/reviewer runners, narrows `role_to_expected` to architect only (coder/reviewer/deployer → opencode; tester is shell-only; judge rides architect's LLM handle), and asserts `deployer` is absent from `pool.jsonl`; `tests/test_cli_run.py::test_dry_run_llm_dispatches_by_role` no longer asserts on deployer. Full suite (excluding `tests/test_tester.py` which has 2 pre-existing failures from before H6) → 539 passed, 1 skipped in 67s. `git diff --check` clean |
| 2026-04-21 | Harness 6 Task H6.3c — Reviewer role on opencode runtime | `orchestrator/roles/reviewer.py` rewritten to drop `BaseRole` and drive an opencode session per review pass. `__init__(runner: SessionRunner, state, phase_logger)`. `execute()` builds one combined prompt (acceptance criteria + tester report + git-diff stat), calls `runner.run(prompt)`, emits audit via `emit_session_audit` *before* any failure raise, then parses `result.assistant_text` with the existing verdict regex and writes `REVIEW.md`. Three session-level failures surface as `ReviewerError`: runner exception, non-zero exit, empty assistant text. New `orchestrator/roles/reviewer_dryrun.py:DryRunReviewerRunner` returns a canned APPROVED verdict — engine auto-provides it when `dry-run injection` is set, mirroring the coder dry-run path. `orchestrator/roles/prompts/reviewer/generate.txt` merged system+user into one prompt and notes that read/grep tools are available to the agent. Engine `_reviewer` now resolves via `_session_runner_for("reviewer")`. `cli/run.py` drops the `_DRY_RUN_REVIEWER_REPORT` branch and now builds opencode sessions for `("coder", "reviewer")` from endpoints routing; `web/backend/runner.py` inherits the change. Tests: `tests/test_reviewer.py` rewritten around a `_FakeRunner` (12 tests covering criteria extraction, verdict parsing variants, REVIEW.md writing, runner-exception wrapping, non-zero exit, empty-text rejection, audit emission on success + on failure, `is_critical`, git-diff fallback); `tests/test_integration_h5_two_endpoint_wireup.py` injects a `DryRunReviewerRunner` alongside the coder runner and asserts `reviewer` is now absent from `pool.jsonl`; `tests/test_cli_run.py::test_dry_run_llm_dispatches_by_role` no longer asserts on reviewer. Full suite (excluding pre-existing 2 tester failures) → 541 passed, 1 skipped in 66s. `git diff --check` clean |
| 2026-04-21 | Harness 6 Task H6.4 — audit-trail replacement for opencode sessions | New `orchestrator/lib/opencode_audit.py:emit_session_audit(state, result, *, role, phase, step, prompt, atom_id, attempt)` — one row per assistant message into both `raw-io.jsonl` and `usage.jsonl`, keyed by a fresh `call_id`/`request_id` so `usage_summary.rollup()` still joins them. `raw-io.jsonl` carries the original prompt as `messages[0]`, the assistant text as `response_content`, and `usage.{prompt,completion,reasoning}_tokens` pulled from the opencode `OpencodeTokens`. `usage.jsonl` uses `provider_kind="opencode"`, provider/model from the message, `cost_usd` from the message's reported cost (source=`provider_reported` when non-zero else `unknown`), and a new `runtime.opencode.{session_id,message_index}` group so rollups can partition "this task's work" even when the session produced multiple assistant turns. `OpencodeMessage` gained `created_ms`/`completed_ms` + a `latency_ms` property driven by `info.time` in the export. Coder calls `emit_session_audit` *before* raising on non-zero exit — failed sessions still leave a forensic trail. Error status + `finish_reason="error"` propagate to both files. Tests: new `tests/test_opencode_audit.py` (8 tests — per-assistant-message rows, user messages skipped, prompt/tokens/latency carried through, session metadata in usage, bus `usage_recorded` event fires with the same call_id, failed-session error path, zero-cost → unknown source, raw-io/usage join key bijection). `tests/test_integration_h2_1_rehearsal.py` restored `coder` to `_EXPECTED_ROLES`. Full suite (excluding pre-existing 2 tester failures) → 540 passed, 1 skipped in 65s. `git diff --check` clean |
| 2026-04-21 | Harness 6 Task H6.6 — opencode production-path guardrail | New `tests/test_integration_h6_opencode_pipeline.py` installs a role-aware fake `opencode` binary on `PATH`, runs the real CLI live/config path (`--config`, not `--dry-run`), and proves `cli/run.py` generates per-project `opencode.json` plus production `OpencodeSession` runners for `architect`, fallback `judge`, `coder`, `tester`, `reviewer`, and `deployer`. The fake session writes the expected artifacts and the test asserts `raw-io.jsonl` / `usage.jsonl` contain matching opencode join keys for every SDLC role. `uv run pytest -q tests/test_integration_h6_opencode_pipeline.py` → 1 passed; `uv run pytest -q tests/test_integration_h6_opencode_pipeline.py tests/test_opencode_session.py tests/test_opencode_audit.py` → 17 passed; `git diff --check` clean |
| 2026-04-21 | Harness 6 Task H6.3a — Coder role on opencode runtime | New `SessionRunner` Protocol in `orchestrator/lib/opencode.py` (one method: `run(prompt) -> OpencodeResult`) is the minimum surface a role needs. `orchestrator/roles/coder.py` rewritten around it: one opencode session per sprint-plan task, no more JSON-of-files parsing, no engine-side path validation (opencode's `write`/`edit` tools are sandboxed to `--dir <project>`). The Coder surfaces three session-level failures as `CoderError`: runner exception, non-zero exit, and zero-write session (prompt/provider regression). Coder prompt (`roles/prompts/coder/generate.txt`) rewritten for agent-with-tools flow — "use your tools to write the files, stop when done", no more "return only JSON." Engine: new `session_runners: dict[str, SessionRunner]` kwarg + `_session_runner_for(role)` resolver (explicit injection → auto-build for dry-run → None). New `orchestrator/roles/coder_dryrun.py:DryRunCoderRunner` writes `README.md` and returns a synthesised `OpencodeResult` with a completed `write` tool call so every dry-run test keeps passing without hand-building a fake. Live wiring: new `orchestrator/cli/run.py:_build_opencode_session_runners` writes `opencode.json` via H6.1 then builds one `OpencodeSession` per opencode-driven role, pulling provider + model from that role's first routing entry; `web/backend/runner.py` adopts it via a new `_build_endpoints_cfg_from_env` helper. Tests: `tests/test_coder.py` rewritten (10 tests: topological sort, prompt rendering with architecture + task context, task-packet preference, no-tasks short-circuit, nonzero-exit surfacing, runner-exception wrapping, zero-write rejection, accepts `edit`/`patch` as valid writes); `tests/test_integration_h5_two_endpoint_wireup.py` updated — coder no longer lands in `pool.jsonl` (explicitly asserted absent), H5.3 now injects a `DryRunCoderRunner`; `tests/test_integration_h2_1_rehearsal.py` dropped `coder` from `_EXPECTED_ROLES` (to be re-added when H6.4 rewires raw-io.jsonl to read from opencode sessions); `tests/test_cli_run.py::test_dry_run_llm_dispatches_by_role` no longer asserts on coder branch. `_DRY_RUN_CODER_JSON` + its content-aware dispatch branch deleted from `cli/run.py`. Full suite → 532 passed, 1 skipped (pre-existing 2 `test_tester.py` failures unrelated, same as on `main` HEAD). `git diff --check` clean |
| 2026-04-21 | Harness 6 Task H6.2 — OpencodeSession subprocess adapter | New `orchestrator/lib/opencode.py` drives opencode as a one-shot subprocess: `OpencodeSession.run(prompt)` spawns `opencode run --format json --dir <project> --model <provider>/<model> [--agent <role>] [--session <id>] --dangerously-skip-permissions <prompt>`, line-buffers stdout, and forwards every NDJSON event to an optional `event_sink` (non-JSON banner lines are logged and skipped). `OPENCODE_CONFIG` points at the per-project overlay written by H6.1; `OPENCODE_DISABLE_AUTOUPDATE=1` prevents the phone-home. On exit, runs `opencode export <sessionID>`, strips the human banner before the first `{`, and parses the JSON into a typed tree: `OpencodeResult{session_id, exit_code, directory, messages[OpencodeMessage{role, provider, model, tokens, cost, text_parts, tool_calls}], total_tokens (OpencodeTokens with input/output/reasoning/cache_read/cache_write), total_cost, summary_{additions,deletions,files}, raw_export, raw_events}`; convenience `assistant_text` / `tool_calls` accessors. `OpencodeError` / `OpencodeBinaryMissing` exceptions surface missing binary + runs that emitted no sessionID. Pure `parse_export()` split out for testable data extraction. New `tests/test_opencode_session.py` (8 tests): parse canonical export + tokens + tool calls; parse tolerates missing summary/tokens; subprocess tests drop a Python shim at `<tmp>/bin/opencode` on `PATH` that mimics `run` (NDJSON + banner) and `export <sid>` (banner + JSON) — assert parsed result shape, event-sink forwarding, argv + env (OPENCODE_CONFIG, OPENCODE_DISABLE_AUTOUPDATE) flow through, `OpencodeBinaryMissing` when PATH is empty, `OpencodeError` when the shim exits without a sessionID, `--session` flows through when `continue_session_id` is passed. `.venv/bin/python -m pytest tests/test_opencode_session.py -q` → 8 passed. Full suite → 568 passed, 1 skipped, 2 failed (the 2 tester failures are pre-existing on `main` HEAD, unrelated to H6.2 — confirmed by running against HEAD with no local changes). `git diff --check` clean |
| 2026-04-21 | Harness 5 Task H5.3 — two-endpoint wire-up integration test | New `tests/test_integration_h5_two_endpoint_wireup.py` (2 tests). Test 1 builds an `UpstreamPool` with two endpoints (`big-model`, `small-model`), each wrapping the content-aware dry-run fake; declares per-role routing (architect+coder → big, judge/tester/reviewer/deployer → small) with a per-role model override on coder; drives `Orchestrator(pool=pool).run()` through every phase; asserts every pipeline artefact exists, both endpoints received work, every role's `pool.jsonl` decision records the declared `chosen_endpoint`, and coder's `chosen_model` is the routing override (`qwen3-coder:30b-override`). Test 2 proves fallback: first routed decision records `fell_back=True` with `attempted_endpoints=["flaky", "healthy"]`; every subsequent role lands on `healthy` after flaky is flipped unhealthy. Caught one flake-during-wiring subtlety: pre-role phases (memory_refresh, persona_forum) have no routing and log as `no_routing_configured` — filter to routed decisions before asserting on fallback. `uv run pytest -q tests/test_integration_h5_two_endpoint_wireup.py` → 2 passed (5s). Full suite → 555 passed, 1 skipped. `git diff --check` clean. **Phase H5 complete.** |
| 2026-04-21 | Harness 5 Task H5.2 — declarative endpoints + multi-provider | New `orchestrator/lib/endpoints.py` parses an `endpoints:` + `routing:` YAML block into `EndpointSpec` / `RouteEntry` / `EndpointsConfig`, resolves `api_key_env` / inline `api_key`, applies `ORCHESTRATOR_ENDPOINT_<NAME>_{BASE_URL,MODEL,API_KEY}` overrides, and materialises a pool via `build_pool()`. `LLMClient.__init__` parameterised on `model` / `api_key` / `extra_body` (sentinel default preserves the Qwen `enable_thinking:False` hint) / `default_headers`; `chat()` accepts a per-call `model=` override. `UpstreamPool.__init__` normalises routing entries (str → `RouteEntry`) and `chat()` computes `model_for_call = route.model or caller_model or endpoint_default`, records it as `chosen_model` in `pool.jsonl`. Wiring: `Config.from_yaml` now drops unknown top-level keys so `endpoints:` / `routing:` don't collide with the dataclass; `orchestrator/cli/run.py` picks `dry-run` / `live/pool[...]` / `live/single-endpoint` mode off the loaded endpoints config; `web/backend/runner.py:_build_pool_from_env` reads `ORCHESTRATOR_CONFIG` and hands a pool to the engine when present. `config.example.yaml` grew a commented example block covering `local-gpu` + `openrouter` with per-role model override. Source comments referencing "primary/alien" as canonical labels replaced with operator-name wording (`orchestrator/lib/legacy pool module:22`, `orchestrator/lib/logger.py:168`). New tests: `tests/test_endpoints.py` (10 tests — missing-field raises, unresolved env raises, duplicate names raise, unknown routing endpoint raises, env overrides win, inline key accepted, YAML roundtrip, `build_pool` wires `default_headers` + `extra_body` + `api_key`); `tests/test_legacy pool module` +3 (route model override wins; caller model fallback; endpoint-default fallback); `tests/test_integration_h3_pool_observability.py` +1 (`chosen_model` lands in `pool.jsonl` per role). `uv run pytest -q` → 553 passed, 1 skipped. `git diff --check` clean. **H5.3 (two-endpoint end-to-end wire-up test against live endpoints) still pending** |
| 2026-04-21 | SRS mini-phase pack created | New `project-management/srs-mini-phases/` directory with `README.md`, `EXECUTION_CHECKLIST.md`, `QUALITY_GATES.md`, `TRACEABILITY.md`, and 12 mini-phase files `M01…M12-*.md` decomposing `docs/srs-council-engine.md` + `docs/pwa-narrative-replay-and-intervention-design.md` into loadable executable phases (event ledger, intent lifecycle, mythos/founding team, narrative digest/DWA, narrator agent, PWA lenses, artifact previews, memory spine, cost/budget telemetry, hookability, replay resilience, release hardening). `project-management/README.md` links the pack without removing existing content. Docs-only change — no source/tests touched; `git diff --check` clean after Codex verification. No tests executed |
| 2026-04-21 | Harness 1 Task H1.2 — inject registry from runner | `web/backend/runner.py` now pulls the per-project `GateRegistry` from `get_gate_store().ensure(project_id)` and passes it into `Orchestrator(gate_registry=...)`. Engine's lazy fallback kept but emits a prominent WARNING so mis-wired code is visible. New `web/backend/tests/test_gate_engine_integration.py` proves round-trip: engine thread calls `_gate()`, HTTP POST decides, engine thread unblocks (approval) or raises `PhaseFailure` with the reason (rejection); third test asserts `routes.gates.get_gate_store() is runner.get_gate_store()`. Fixed TestClient LAN-middleware block via `ORCHESTRATOR_EXPOSE_PUBLIC=1`; test threads made daemon. Updated `web/backend/tests/test_runner.py` fakes to accept `gate_registry` kwarg. `uv run pytest -q web/backend/tests/test_gate_engine_integration.py` → 3 passed. Full suite → 503 passed, 1 skipped. `git diff --check` clean. H1.3 reshaped around user feedback: PWA is an idle-RPG touch surface — gating is opt-in per run, not coerced on |
